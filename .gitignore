# dependencias
node_modules/
.pnp
.pnp.js

# build
.next/
out/
build/
dist/

# entorno — NUNCA subir credenciales
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# sistema
.DS_Store
Thumbs.db
*.log
npm-debug.log*
yarn-error.log*

# editores
.idea/
.vscode/*
!.vscode/extensions.json

# drizzle
drizzle/meta/_journal.json.bak

# base de datos local de pruebas
.pglite/
.env*
.env.local.respaldo

# Los vídeos de origen/ no van a git.
# Son material de partida, como los .docx, pero pesan cientos de megas: uno
# solo pasaba de los 100 MB con los que GitHub corta el push. Y no hacen
# falta en el repositorio — la sección del autor guarda ENLACES de YouTube o
# Vimeo, no archivos, justo por esto mismo.
origen/*.mp4
origen/*.mov
origen/*.MP4
origen/*.MOV
